import { PopupLoaderDirective } from './popup-loader.directive';

describe('PopupLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
